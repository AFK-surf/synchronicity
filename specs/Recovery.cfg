\* The connected cluster: summaries reach the node before a post-loss
\* publish and the quiesce reaches every peer.  Every property must hold.
SPECIFICATION Spec
CONSTANTS
    PEERS = {p1, p2}
    ROOTS = {1, 2}
    MAX_SEQ = 4
    GAP = 1
    MAX_CRASHES = 2
    PARTITIONED = FALSE
INVARIANTS
    TypeOK
    NoObservableFork
PROPERTIES
    FloorMonotone
    SlotMonotone
    HeadMonotone
